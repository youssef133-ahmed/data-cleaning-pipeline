# data-cleaning-pipeline
This project demonstrates a simple data cleaning pipeline using Python and Pandas. The script reads a dataset containing employee information, removes duplicate records, handles missing values, and exports a cleaned dataset ready for analysis.
